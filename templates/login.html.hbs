<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/app.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
</head>
<body class="bg-light">
    <div class="d-flex justify-content-center align-content-center flex-wrap">
        <div class="login_box">
            <h1>Login</h1>
            {{#if error_invalid}}
                <div class="alert alert-danger" role="alert">
                    Invalid email or password. Please try again.
                </div>
            {{/if}}
            {{#if error_contact-admin}}
                <div class="alert alert-danger" role="alert">
                    Somethings wrong with your account, please contact the administrator!
                </div>
            {{/if}}
            {{#if error_too-many-attempts}}
                <div class="alert alert-danger" role="alert">
                    Too many failed login attempts, please wait 15 minutes or reset your password!
                </div>
            {{/if}}
            {{#if error_pw-reset}}
                <div class="alert alert-success" role="alert">
                    If an account with the specified email address exists, you will receive a password reset email soon.
                </div>
            {{/if}}
            {{#if error_pw-reset-success}}
                <div class="alert alert-success" role="alert">
                    Your password was chanaged successfully! You can now login with your new password.
                </div>
            {{/if}}
            <form method="post">
                <div class="form-group mt-3">
                    <label for="email">Email address</label>
                    <input type="email" class="form-control" name="email" id="email" aria-describedby="emailHelp" placeholder="Enter email">
                </div>
                <div class="form-group mb-3">
                    <label for="password">Password</label>
                    <input type="password" class="form-control" name="password" id="password" placeholder="Password">
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
                <a class="float-end" href="/pw-reset">Reset Password</a>
            </form>

        </div>
    </div>
</body>
</html>