<div class="row">
    <div class="col-6">
        Include posts from categories
        {{#each categories}}
            {{name}}
        {{/each}}
    </div>
    <div class="col-6">
        Exclude posts from categories
    </div>
</div>